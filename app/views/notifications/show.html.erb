<div class="max-w-2xl mx-auto px-4 py-8">
  <h1 class="text-3xl font-display font-bold text-sf-black dark:text-white mb-6">Notifications</h1>

  <% if @unread_conversations.empty? && @unread_cohorts.empty? && @unread_post_cohorts.empty? && @unread_comment_posts.empty? %>
    <div class="text-center py-16 text-gray-400 dark:text-gray-500">
      <svg class="w-12 h-12 mx-auto mb-4 text-gray-300 dark:text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/>
      </svg>
      <p class="text-lg">All caught up!</p>
      <p class="text-sm mt-2">No unread messages.</p>
    </div>
  <% else %>
    <% if @unread_conversations.any? %>
      <h2 class="text-lg font-display font-semibold text-sf-black dark:text-white mb-3">Private Messages</h2>
      <div class="space-y-2 mb-8">
        <% @unread_conversations.each do |conversation| %>
          <% other = conversation.other_participant(current_user) %>
          <% next unless other %>
          <% unread = conversation.unread_count(current_user) %>
          <%= link_to conversation_path(conversation),
              class: "block bg-white dark:bg-gray-800 rounded-2xl shadow-sm border border-sf-sand/20 dark:border-gray-700 p-4 hover:shadow-md transition",
              data: { turbo_prefetch: false } do %>
            <div class="flex items-center gap-4">
              <div class="w-10 h-10 rounded-full bg-sf-sand/50 dark:bg-gray-700 flex items-center justify-center text-sm font-bold text-sf-black dark:text-white flex-shrink-0">
                <% if other.avatar.attached? %>
                  <%= image_tag other.avatar.variant(:display),
                      class: "w-10 h-10 rounded-full object-cover" %>
                <% else %>
                  <%= other.name.first.upcase %>
                <% end %>
              </div>
              <div class="flex-1 min-w-0">
                <p class="font-semibold text-sf-black dark:text-white truncate"><%= other.name %></p>
                <p class="text-sm text-gray-500 dark:text-gray-400">Private message</p>
              </div>
              <span class="bg-sf-red text-white text-xs font-bold rounded-full w-6 h-6 flex items-center justify-center flex-shrink-0"><%= unread %></span>
            </div>
          <% end %>
        <% end %>
      </div>
    <% end %>

    <% if @unread_cohorts.any? %>
      <h2 class="text-lg font-display font-semibold text-sf-black dark:text-white mb-3">Group Chats</h2>
      <div class="space-y-2 mb-8">
        <% @unread_cohorts.each do |cohort| %>
          <% unread = cohort.unread_count(current_user) %>
          <%= link_to cohort_path(cohort),
              class: "block bg-white dark:bg-gray-800 rounded-2xl shadow-sm border border-sf-sand/20 dark:border-gray-700 p-4 hover:shadow-md transition",
              data: { turbo_prefetch: false } do %>
            <div class="flex items-center gap-4">
              <div class="w-10 h-10 rounded-full bg-sf-gold/20 dark:bg-sf-gold/10 flex items-center justify-center text-sm font-bold text-sf-gold flex-shrink-0">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/>
                </svg>
              </div>
              <div class="flex-1 min-w-0">
                <p class="font-semibold text-sf-black dark:text-white truncate"><%= cohort.name %></p>
                <p class="text-sm text-gray-500 dark:text-gray-400">Group chat</p>
              </div>
              <span class="bg-sf-red text-white text-xs font-bold rounded-full w-6 h-6 flex items-center justify-center flex-shrink-0"><%= unread %></span>
            </div>
          <% end %>
        <% end %>
      </div>
    <% end %>

    <% if @unread_post_cohorts.any? %>
      <h2 class="text-lg font-display font-semibold text-sf-black dark:text-white mb-3">New Posts</h2>
      <div class="space-y-2 mb-8">
        <% @unread_post_cohorts.each do |cohort| %>
          <% unread = cohort.unread_post_count(current_user) %>
          <%= link_to cohort_path(cohort, tab: :feed),
              class: "block bg-white dark:bg-gray-800 rounded-2xl shadow-sm border border-sf-sand/20 dark:border-gray-700 p-4 hover:shadow-md transition",
              data: { turbo_prefetch: false } do %>
            <div class="flex items-center gap-4">
              <div class="w-10 h-10 rounded-full bg-sf-gold/20 dark:bg-sf-gold/10 flex items-center justify-center text-sm font-bold text-sf-gold flex-shrink-0">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"/>
                </svg>
              </div>
              <div class="flex-1 min-w-0">
                <p class="font-semibold text-sf-black dark:text-white truncate"><%= cohort.name %></p>
                <p class="text-sm text-gray-500 dark:text-gray-400"><%= "new post".pluralize(unread) %></p>
              </div>
              <span class="bg-sf-red text-white text-xs font-bold rounded-full w-6 h-6 flex items-center justify-center flex-shrink-0"><%= unread %></span>
            </div>
          <% end %>
        <% end %>
      </div>
    <% end %>

    <% if @unread_comment_posts.any? %>
      <h2 class="text-lg font-display font-semibold text-sf-black dark:text-white mb-3">New Comments</h2>
      <div class="space-y-2">
        <% @unread_comment_posts.each do |post| %>
          <% unread = post.unread_comment_count(current_user) %>
          <%= link_to cohort_post_path(post.cohort, post),
              class: "block bg-white dark:bg-gray-800 rounded-2xl shadow-sm border border-sf-sand/20 dark:border-gray-700 p-4 hover:shadow-md transition",
              data: { turbo_prefetch: false } do %>
            <div class="flex items-center gap-4">
              <div class="w-10 h-10 rounded-full bg-sf-gold/20 dark:bg-sf-gold/10 flex items-center justify-center text-sm font-bold text-sf-gold flex-shrink-0">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
                </svg>
              </div>
              <div class="flex-1 min-w-0">
                <p class="font-semibold text-sf-black dark:text-white truncate"><%= post.body&.truncate(60) %></p>
                <p class="text-sm text-gray-500 dark:text-gray-400"><%= "new comment".pluralize(unread) %> in <%= post.cohort.name %></p>
              </div>
              <span class="bg-sf-red text-white text-xs font-bold rounded-full w-6 h-6 flex items-center justify-center flex-shrink-0"><%= unread %></span>
            </div>
          <% end %>
        <% end %>
      </div>
    <% end %>
  <% end %>
</div>
