<%= render "shared/post_edit_form",
    post: @post,
    back_path: cohort_path(@cohort, tab: :feed),
    autosave_url: cohort_post_path(@cohort, @post),
    form_url: cohort_post_path(@cohort, @post) %>
