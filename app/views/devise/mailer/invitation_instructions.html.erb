<%= render "shared/invitation_email_body",
    recipient_name: @resource.name.presence,
    custom_message: @resource.bulk_invitation&.message,
    accept_url: accept_invitation_url(@resource, invitation_token: @token),
    due_at: @resource.invitation_due_at %>
